@using TravoRides.Application.DTOs.Common
@using TravoRides.Application.DTOs.FeaturesMaster
@model PagedResponse<FeaturesMasterDTO>

@if (Model.Items.Count() > 0)
{
    <thead>
        <tr>
            <th style="width: 10%">
                S.No
            </th>
            <th style="width: 20%">
                Title
            </th>

            <th style="width: 50%" class="text-center">
                Description
            </th>
            <th style="width: 10%" class="text-center">
                Actions
            </th>
            <th style="width: 10%" class="text-center">
                View
        </tr>
    </thead>

    <tbody>
        @{ int count = 1;}
        @foreach (var FeaturesMaster in Model.Items)
        {
            <tr>
                <td>
                    @(count++)
                    <input type="hidden" class="hdn-id" value="@FeaturesMaster.Id" />

                </td>

                <td class="name">@FeaturesMaster.Title</td>

               <td>
                    @{ var fb = FeaturesMaster.Description ?? string.Empty; }
                    @((fb.Length > 50) ? fb.Substring(0, 50) + "..." : fb)
                </td>

                <td class="project-actions text-center">

                    <a asp-controller="FeatureMaster" asp-action="Edit" asp-route-id="@FeaturesMaster.Id" class="btn btn-warning btn-sm" data-toggle="tooltip" data-placement="top" title="Edit">
                        <i class="fas fa-pencil-alt"></i>
                    </a>
                    <a class="btn btn-danger btn-sm btn-delete" href="#" data-toggle="modal" data-target="#modal-delete" title="Delete">
                        <i class="fas fa-trash"></i>
                    </a>
                </td>
                <td class="text-center">
                    <button type="button" class="btn btn-info btn-sm btn-view" onclick="showReviewDetails(this)"
                            data-id="@FeaturesMaster.Id"
                            data-title="@FeaturesMaster.Title"
                            data-icon="@FeaturesMaster.Icon"
                            data-description="@FeaturesMaster.Description"
                           >
                        View
                    </button>
                    </td>
            </tr>
        }



    </tbody>
}
else
{
    <tr>
        <td>Sorry! No features found</td>
    </tr>}
