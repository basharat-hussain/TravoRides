@using TravoRides.Application.DTOs.Common
@model PagedResponse<CabDTO>


@if (Model.Items.Count() > 0)
{
    <thead>
        <tr>
            <th style="width: 10%">
                S.No
            </th>
            <th style="width: 20%">
                Category
            </th>
            <th style="width: 30%">
                CabName
            </th>


            <th style="width: 20%">
                Price
            </th>
            <th style="width: 20%">
                 Discount
            </th>
           
            <th style="width: 10%" class="text-center">
                Actions
            </th>
           <th style="width: 10%" class="text-center">
                View
            </th>
        </tr>
    </thead>

    <tbody>
        @{ int count = 1;}
        @foreach (var Cab in Model.Items)
        {
            <tr>
                <td>
                    @(count++)
                    <input type="hidden" class="hdn-id" value="@Cab.Id" />

                </td>
                <td>@Cab.Category.Name</td>
                <td class="name">@Cab.Name</td>

                <td>@Cab.SelfDrive.PricePerDay </td>
               <td>@Cab.SelfDrive.Discount</td>


                <td class="project-actions text-center">

                    <a asp-controller="SelfDrive" asp-action="Edit" asp-route-id="@Cab.Id" class="btn btn-warning btn-sm" data-toggle="tooltip" data-placement="top" title="Edit">
                        <i class="fas fa-pencil-alt"></i>
                    </a>
                    <a class="btn btn-danger btn-sm btn-delete" href="#" data-toggle="modal" data-target="#modal-delete" title="Delete">
                        <i class="fas fa-trash"></i>
                    </a>
                </td>
                  <td class="text-center">
                    <button type="button" class="btn btn-info btn-sm btn-view" onclick="showReviewDetails(this)"
                            data-id="@Cab.Id"
                            data-category="@Cab.Category.Name"
                            data-name="@Cab.Name"
                            data-price="@Cab.SelfDrive.PricePerDay"
                            data-discount="@Cab.SelfDrive.Discount"
                           
                           >
                        View
                    </button>
                    </td>
            </tr>
        }



    </tbody>
}
else
{
    <tr>
        <td>Sorry! No SelfDrives found</td>
    </tr>}
