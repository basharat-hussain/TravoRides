@using TravoRides.Application.DTOs.Transit
@using TravoRides.Application.DTOs.Common
@model PagedResponse<TransitDTO>


@if (Model.Items.Count() > 0)
{
    <thead>
        <tr>
            <th style="width: 10%">
                S.No
            </th>

            <th style="width: 20%">
                Title
            </th>

            <th style="width: 15%">
                Price
            </th>
            <th style="width: 15%">
                 Discount
            </th>
           
            <th style="width: 10%" class="text-center">
                Actions
            </th>
           <th style="width: 10%" class="text-center">
                View
            </th>
        </tr>
    </thead>

    <tbody>
        @{ int count = 1;}
        @foreach (var Transit in Model.Items)
        {
            <tr>
                <td>
                    @(count++)
                    <input type="hidden" class="hdn-id" value="@Transit.Id" />

                </td>

                <td class="name">@Transit.Title</td>

                <td>@Transit.Price </td>
               <td>@Transit.Discount</td>


                <td class="project-actions text-center">

                    <a asp-controller="Transit" asp-action="Edit" asp-route-id="@Transit.Id" class="btn btn-warning btn-sm" data-toggle="tooltip" data-placement="top" title="Edit">
                        <i class="fas fa-pencil-alt"></i>
                    </a>
                    <a class="btn btn-danger btn-sm btn-delete" href="#" data-toggle="modal" data-target="#modal-delete" title="Delete">
                        <i class="fas fa-trash"></i>
                    </a>
                </td>
                  <td class="text-center">
                    <button type="button" class="btn btn-info btn-sm btn-view" onclick="showReviewDetails(this)"
                            data-id="@Transit.Id"
                            data-title="@Transit.Title"
                            data-price="@Transit.Price"
                            data-discount="@Transit.Discount"
                           
                            data-description="@Transit.Description"
                            data-image="@Transit.ImageUrl"
                           >
                        View
                    </button>
                    </td>
            </tr>
        }



    </tbody>
}
else
{
    <tr>
        <td>Sorry! No Transits found</td>
    </tr>}
